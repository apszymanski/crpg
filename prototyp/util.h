
#ifndef UTIL_H_
#define UTIL_H_

#include <cstdlib>
#include <iostream>
#include <string>

int int_array_sum(int*, size_t);
int int_array_min_index(int*, size_t);
int reassign(int*, size_t, uint, uint);
void print_ability_score(std::string*, int*, size_t, bool);

#endif